export const APP_TITLE = 'Accion Digital SPA';
export const APP_SUBTITLE = 'Migracion incremental en progreso.';

export function buildMigrationBanner(title, subtitle) {
	return `${title} - ${subtitle}`;
}
